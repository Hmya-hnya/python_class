# 문자열(파이썬에는 문자타입은 존재하지 않음), 숫자(정수형, 실수형), Boolean(참, 거짓)
# 변수: 자료형을 이용해 실제 데이터를 저장 할 공간을 할당 받는 것
# 변수에 값을 대입하는 연산자: '='  ※ 등호는 '=='

a = b = c = 1
a, b, c = 1, True, "곰돌이" # 파이썬에서만 가능 / 다른 언어에서는 불가능함

# 변수의 타입 확인: type 으로 확인 가능
print(type(a)) #정수(int)
print(type(b)) #참거짓(bool)
print(type(c)) #문자열(str)

